[project]
name = "blogging-platform-api"
version = "1.0.0"
description = "Blogging Platform API Project from roadmap.sh"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "sqlmodel>=0.0.24",
]
